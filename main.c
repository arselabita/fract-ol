#include "fractol.h"

int main(int ac, char **av)
{
    init_window_and_display();
    if (ac == 4)
        ft_fractol(arv[1],);
    else
        write(1, "Erorrrrrrrr write Mandelbrot or Juliaaaa :)", 44);
    return (0);
}